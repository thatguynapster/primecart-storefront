import { ImagePreviewSlider } from '@/components/image-preview-slider'
import { productImages } from '@/dummyData'
import React from 'react'

type Props = {
    params: { product_id: string }
}

const page = ({ params: { product_id } }: Props) => {
    return (
        <div className="px-4">

        </div>
    )
}

export default page